{
  "name": "railway-reservation-system",
  "version": "1.0.0",
  "description": "Root package.json for Railway.app Monorepo deployment",
  "scripts": {
    "postinstall": "npm install --prefix server",
    "start": "npm start --prefix server"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
